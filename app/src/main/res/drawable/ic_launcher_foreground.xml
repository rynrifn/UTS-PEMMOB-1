<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.flashnews">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar">

        <!-- Splash screen sebagai launcher -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.Flashnews">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Activity login -->
        <activity
            android:name=".LoginActivity"
            android:exported="true" />

        <!-- Activity register -->
        <activity
            android:name=".RegisterActivity"
            android:exported="true" />

        <!-- Dashboard berita dengan RecyclerView -->
        <activity
            android:name=".NewsDashboardActivity"
            android:exported="true" />

        <!-- MainActivity (opsional digunakan atau tidak) -->
        <activity
            android:name=".MainActivity"
            android:exported="true" />

    </application>

</manifest>
